require 'spec_helper'

describe CodeStyleConfiguration do
  it 'has a version number' do
    expect(CodeStyleConfiguration::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
